#include <stdio.h>

int main(void){
  int num;
  char decision;
  
  printf("Enter number: ");    // ?? tama ba na number ?? hehe pero pwede pa rin naman'g ma-modify 'to hehe
  scanf("%d", &num);
         
  do(                          // do-while 'yong mairerecom ko para sa program na 'to hehe sksk pero kung may mas better, open ako for changes hehe uwu
    printf("Enter digits: ");  // saasdmaksm digits ba ?? HUHU
    scanf(
      
    // arranging the digits to ascending order
      
    // finding the index of the value inserted by user
      
    )
    while(decision = "y");
}
